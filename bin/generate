#!/usr/bin/env node

var generate = require(__dirname + '/../generate');

var content = '';
process.stdin.resume();
process.stdin.on('data', function(buf) { content += buf.toString(); });
process.stdin.on('end', function() {
  console.log(generate('WhileStatement'));
});
