(function check(window) {
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }

}(function autoScrollWatch(values) {
      scope.optDisabled = elem.elem;
}));

(function check(window) {
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }
      function sortedKeys(results) {
                return Sizzle(locals);
                    }

}(function autoScrollWatch(values) {
      scope.optDisabled = elem.elem;
}));
